{
  "name": "@q8t/salesforce-sdk",
  "version": "0.0.1",
  "private": true,
  "description": "Comprehensive Salesforce API SDK with support for multiple authentication methods and API types",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "tsx src/generator/index.ts",
    "build": "pnpm generate && tsc",
    "dev": "tsc --watch",
    "lint": "biome check .",
    "format": "biome format --write .",
    "check": "biome check . && tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "dependencies": {
    "yaml": "^2.3.4",
    "jsonwebtoken": "^9.0.2",
    "@q8t/effect-sdk-base": "workspace:*",
    "effect": "^3.10.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "@types/jsonwebtoken": "^9.0.5",
    "tsx": "^4.7.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.0"
  }
}
